{
  "setup_script": "cd esp32_firmware/webui && npm install && cd ../.. && pip install pytest -r examples/requirements.txt && pio pkg install",
  "dev_script": "cd esp32_firmware/webui && npm run dev",
  "verification_script": "cd esp32_firmware/webui && npx tsc --noEmit && cd ../../examples && python3 -m pytest test_*.py -v"
}
