{
  "name": "wireless-pov-poi",
  "private": true,
  "workspaces": [
    "firmware/esp32_firmware/webui"
  ],
  "scripts": {
    "webui:dev": "npm run dev --workspace firmware/esp32_firmware/webui",
    "webui:build": "npm run build --workspace firmware/esp32_firmware/webui",
    "webui:native:android": "npm run native:android --workspace firmware/esp32_firmware/webui",
    "webui:native:ios": "npm run native:ios --workspace firmware/esp32_firmware/webui"
  }
}
