# AI Agent System Requirements
# Minimal dependencies for analysis, testing, and pattern generation

# Image processing (required by examples/tests)
Pillow>=9.0.0

# Task parsing
PyYAML>=6.0

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0

# PlatformIO for firmware builds (optional, skip with SKIP_PIO=1)
platformio>=6.0.0
