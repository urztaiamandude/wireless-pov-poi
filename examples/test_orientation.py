from PIL import Image\n\n# Create a new image with RGB mode\nwidth, height = 64, 31\nimage = Image.new('RGB', (width, height))\n\n# Generate colors for each section of the image\nfor y in range(31):\n    if 0 <= y <= 10:\n        image.paste((255, 0, 0), (0, y, width, y + 1))  # Red\n    elif 11 <= y <= 20:\n        image.paste((0, 255, 0), (0, y, width, y + 1))  # Green\n    elif 21 <= y <= 30:\n        image.paste((0, 0, 255), (0, y, width, y + 1))  # Blue\n\n# Save the image as test_orientation.png\nimage.save('test_orientation.png')\n